<span class="tagcloud">
  <h4>Kategorie anzeigen</h4>
  <% tag_cloud tags, %w(css1 css2 css3 css4) do |tag, css_class| %>
      <%= link_to "#{tag.name} (#{tag.count})", { :action => :tag, :id => tag.name }, :class => css_class %>
  <% end %>
</span>
